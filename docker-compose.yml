version: '3'

networks:
  laohuang:

services:
  laohuangli:
    build: .
    volumes:
      - ./db:/db
      - /etc/ssl/certs:/etc/ssl/certs
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - laohuang
